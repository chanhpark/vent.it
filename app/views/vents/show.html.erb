<div class="large-9 column">
  <div class="post beforepagination">
    <div class="userinfo pull-left">
      <h4>#<%= @vent.id %></h4>
    </div>
    <div class="posttext pull-left">
      <h4><%= @vent.title %></h4>
      Shared by Anonymous
        <p>
          <%= @vent.content %>
        </p>
    </div>
    <div class="clearfix"></div>
      <%= render "showpostinfobot" %>
      <%= render "replies" %>
      <%= render "comments/form" %>
  </div>
</div>
